# air_quality_data_assignment
This project implements an end-to-end data pipeline for processing air quality data using a Medallion Architecture (Bronze → Silver → Gold) on AWS.

install pandas
install requests
install boto3
